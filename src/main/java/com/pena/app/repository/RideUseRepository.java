package com.pena.app.repository;

import com.pena.app.entity.RideUse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RideUseRepository extends JpaRepository<RideUse, Long> {
}
